# Label Normalization

Normalization converts inconsistent label-like inputs into a single 812x1218 dot canvas.

## Future Behavior

- Detect input type.
- Preserve the original file.
- Render or load the input into an image.
- Apply profile defaults.
- Allow manual crop and rotation overrides.
- Save a preview image before printing.

Automatic printing should stay disabled until preview is reliable.
